package com.example.platform.downloader.domain.enums;

/** Loại credential mà `ProviderCredential` hỗ trợ ở v1. */
public enum CredentialType {
    /** Cookie bundle dùng cho `yt-dlp`. */
    COOKIE
}
