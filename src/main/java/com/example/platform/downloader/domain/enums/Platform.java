package com.example.platform.downloader.domain.enums;

/** Danh sách nền tảng mà pipeline downloader hiện hỗ trợ. */
public enum Platform {
    /** Nội dung từ YouTube. */
    YOUTUBE,
    /** Nội dung từ TikTok. */
    TIKTOK,
    /** Nội dung từ Instagram. */
    INSTAGRAM,
    /** Nội dung từ Facebook. */
    FACEBOOK
}
